import react, { useEffect, useRef } from "react";

function createTitle(props) {
   
   
   
    return(
        <h1 style = {{color: `${props.color}`}}>Электронный сомелье</h1>
    )
}

export default createTitle;